apiVersion: apps/v1
kind: Deployment
metadata:
    name: assertly-ai-agent-service-deployment
    labels:
        name: assertly-ai-agent-service-deployment
        app: assertly
spec:
    replicas: 1
    selector:
        matchLabels:
            app: assertly
    template:
        metadata:
            name: assertly-ai-agent-service-pod
            labels:
                app: assertly
                name: assertly-ai-agent-service-pod
        spec:
            containers:
                - name: assertly-ai-agent-service-pod
                  image: 381492239882.dkr.ecr.us-west-2.amazonaws.com/assertly-ai-agent-service:latest
                  ports:
                      - containerPort: 8000
                  env:
                      - name: PORT
                        value: '8000'
                  envFrom:
                      - secretRef:
                            name: assertly-ai-agent-service-secrets
