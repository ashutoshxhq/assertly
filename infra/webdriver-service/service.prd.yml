apiVersion: v1
kind: Service
metadata:
    name: webdriver-service
    labels:
        app: webdriver-service
spec:
    type: ClusterIP
    selector:
        app: assertly
        name: assertly-webdriver-service-pod
    ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: 8000
